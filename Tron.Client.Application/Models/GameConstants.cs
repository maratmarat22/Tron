namespace Tron.Client.Application.Models
{
    internal enum GameConstants
    {
        LIVES = 3,
        KILL_SCORE = 200,
        ALIVE_SCORE = 100,
        
        GAME_TICK = 25,
        MOVE_COOLDOWN = 15,

        CHANGE_DIRECTION_CHANCE = 5,
        CRITICAL_CHANGE_DIRECTION_CHANGE = 90
    }
}
